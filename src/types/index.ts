/**
 * @deprecated LEGACY TYPES - DO NOT USE IN NEW CODE
 * 
 * This file contains old mock data types used by legacy components.
 * For production code, use the DTOs from /types/dto.ts instead.
 * 
 * Legacy types are kept for backward compatibility with:
 * - /utils/aiService.ts (orphaned)
 * - /utils/mockData.ts (orphaned)
 */

// Re-export all production DTOs
export * from './dto';

// ─── Legacy types (deprecated) ─────────────────────────